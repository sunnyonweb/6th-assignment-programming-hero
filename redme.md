1) What is the difference between var, let, and const?

    those all contain value.
    1.1: var = var have some error and it's like global. every where you can use this values
    1.2: let = let is not global. it permit to reassign it's value
    1.3: const= const can't permit to change it's value

2) What is the difference between map(), forEach(), and filter()?

    2.1: map()=Transform each element into a new array
    2.2:forEach()=Iterate and perform side effects like loop
    2.3:filter()=Select elements that pass a condition

3) What are arrow functions in ES6?

    3.1: ECMAScript 6 is a updated version for javascript and it contain and update various things. arrow function is one of them. arrow function is simplified version of a function . simple example is () => 42

4) How does destructuring assignment work in ES6?

    4.1: destructuring is a poweeerful feature in es6. easy to unpack an array or objects value. codes are simple and fresh

5) Explain template literals in ES6. How are they different from string concatenation?

    5.1: template literals is very usefull template for writing string. it's really easy for multiline. also known as backtick (``).